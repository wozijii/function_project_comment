    多人协同动态拣货问题：
        背景：仓库人员使用老式WMS下发任务进行拣货会出现各种情况（绕路、效率低下、堵车等费时费力）这是一个仓库拣货任务分配与优化系统，应用于仓储物流场景。仓库内有多个员工同时作业，系统需要自动将拣货任务分配给合适的员工，并优化拣货路径。
        解决问题：
            任务分配问题：新任务来了，该分配给哪个员工？插入到该员工任务队列的什么位置？
            路径优化问题：一个任务包含多个SKU，按什么顺序拣货总路径最短？（TSP旅行商问题）
            策略切换问题：支持距离优先、负载均衡、混合策略三种分配模式
            紧急任务处理：紧急任务需要优先插入，不能按普通逻辑排队
            时间预测：预测每个任务的开始和完成时间，支持时间校准
            可视化仿真：生成员工拣货路径的动画视频，直观展示优化效果
        使用的方法（重点）：
            A*算法（PathFinder）：在仓库网格地图上计算两点间最短路径，避开货架障碍物，使用曼哈顿距离作为启发函数
            TSP求解（TSPSolver）：使用Google OR-Tools求解任务内多SKU的最优拣货顺序，限制求解时间5秒
            贪心插入策略（TaskAllocator）：对每个新任务，遍历所有员工的所有可能插入位置，计算成本（时间/距离），选择最优
            坐标转换+修正：真实坐标(米) ↔ 网格坐标互转，并用snap_to_nearest_free把落点修正到最近的可行走通道
            缓存机制：TSP结果缓存 + A*距离缓存，避免重复计算
            轮次管理：每个simulate_id独立目录存储数据，支持多轮次推送和增量合并
        作用：
            自动化任务分配：替代人工调度，根据策略自动把任务分配给最合适的员工
            缩短拣货路径：通过TSP优化SKU顺序，减少员工无效行走距离
            支持多种策略：可根据实际需求在"全局距离最短"和"员工负载均衡"之间切换
            提供可视化：生成MP4动画展示优化前后的拣货路径对比，便于验证和汇报
            前后端联调：通过FastAPI提供REST接口，支持前端实时推送数据和接收回调通知